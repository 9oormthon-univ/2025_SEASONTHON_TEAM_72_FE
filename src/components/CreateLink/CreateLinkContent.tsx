import styled from "styled-components";

const CreateLinkContent = () => {
  return (
    <CreateLinkPageLayout>
      <span>this is create link page.</span>
    </CreateLinkPageLayout>
  );
};

export default CreateLinkContent;

const CreateLinkPageLayout = styled.div``;
