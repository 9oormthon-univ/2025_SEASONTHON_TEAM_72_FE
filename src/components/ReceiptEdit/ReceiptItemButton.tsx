import React from 'react';
import styled from "styled-components";
import { CiCirclePlus } from "react-icons/ci";

const ReceiptItemButton: React.FC = () => {
    return (
        <ItemContainer>
            
        </ItemContainer>
    );
};

export default ReceiptItemButton;

const ItemContainer = styled.div`
`;